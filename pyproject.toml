[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "veriflow"
version = "0.1.0"
description = "Evidence-driven, LLM-assisted vulnerability discovery"
readme = "README.md"
requires-python = ">=3.12,<3.15"
dependencies = [
  "alembic>=1.16,<2",
  "pydantic>=2.11,<3",
  "sqlalchemy>=2.0.41,<2.1",
  "tree-sitter>=0.25,<0.26",
  "tree-sitter-c>=0.24,<0.25",
  "tree-sitter-c-sharp>=0.23,<0.24",
  "tree-sitter-cpp>=0.23,<0.24",
  "tree-sitter-go>=0.25,<0.26",
  "tree-sitter-java>=0.23,<0.24",
  "tree-sitter-javascript>=0.25,<0.26",
  "tree-sitter-rust>=0.24,<0.25",
  "tree-sitter-typescript>=0.23,<0.24",
  "typer>=0.16,<1",
]

[project.scripts]
veriflow = "veriflow.cli:app"
traceproof = "veriflow.cli:app"

[project.optional-dependencies]
gcs = [
    "google-cloud-storage>=3.4,<4",
]

[dependency-groups]
dev = ["pytest>=8.4,<10", "ruff>=0.12,<1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
target-version = "py312"
line-length = 100
extend-exclude = ["tests/fixtures"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.per-file-ignores]
"src/veriflow/cli.py" = ["B008"]
"tests/fixtures/joern-flask*/**" = ["E501", "F401", "F811", "F841", "I001"]
